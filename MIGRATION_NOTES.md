# Framework Guide review update — v0.3.1

Version 0.3.1 keeps the Guide-first application boundary established in v0.2.0 and completes the next review pass:

- The sidebar search is a single modern field with its icon inside the input.
- Section groups use styled React controls with animated collapse, active-group expansion and session persistence.
- The Guide now contains nineteen detailed sections instead of the earlier fourteen-section summary.
- Interactive flip cards reveal implementation tips and terminal-style command examples by mouse or keyboard.
- Framework content is aligned with the bundled v0.4.0 build report, public API inventory, Automation inventory, Swagger inventory and established framework contracts.
- The Guide explicitly records evidence caveats, including the dirty feature-branch build and the current branch-coverage gap.
- QADBUtility remains unchanged and disconnected.
